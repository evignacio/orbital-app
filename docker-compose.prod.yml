services:
  api:
    build: ./api
    ports:
      - "3001:3001"
    env_file: ./api/.env.prod
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"
    restart: unless-stopped
